---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/hiera
:hierarchy:
  - hosts/%{fqdn}
  - profiles/%{role}
  - os/%{osfamily}-%{operatingsystem}
  - os/%{osfamily}
  - hostgroups/%{hostgroup}
  - default

